{
  "name": "awesome-popular-repos",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "GitHub 热门项目展示平台 - 收藏和展示优秀的开源项目",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pages:dev": "npm run build && wrangler pages dev dist --d1 DB=awesome-repos-db",
    "pages:deploy": "npm run build && wrangler pages deploy dist",
    "db:create": "wrangler d1 create awesome-repos-db",
    "db:init": "wrangler d1 execute awesome-repos-db --file=./schema.sql",
    "db:init:local": "wrangler d1 execute awesome-repos-db --file=./schema.sql --local"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "wrangler": "^3.0.0"
  }
}
